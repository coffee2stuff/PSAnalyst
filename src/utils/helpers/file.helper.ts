export class FileHelper {
    //private mimeTypeRegex: RegExp = /^(application|audio|example|image|message|model|multipart|text|video)\/[a-z0-9.+*-]+$/;
    //private extensionRegex: RegExp = /\.[a-zA-Z0-9]*$/;
    //private setFileExtension() {}
}
